import { Table, Tag, Space } from 'antd';

function DataTable() {
  return (
    <div>
    // <Table columns={columns} dataSource={data} />
      Hello from Table
    </div>
  )
}

export default DataTable;